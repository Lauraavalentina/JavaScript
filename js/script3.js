function Asistencia() {
    let asiste = document.getElementById("respuesta").value;
  
    if (asiste === "presente") {
        alert("El estudiante está presente");
      }

      else {
        alert("El estudiante no está presente");
      }
    } 

    function mama(){
        let respuesta = "pase";

        if (respuesta === "pase"){
            alert("Felicitarlo")
        } else {
            alert("Le pega")
        }
    }